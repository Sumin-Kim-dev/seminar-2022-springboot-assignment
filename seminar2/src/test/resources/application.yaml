spring:
  profiles:
    active: local
  jpa:
    properties:
      hibernate:
        show_sql: true
    hibernate:
      ddl-auto: create-drop

auth.jwt:
  jwtSecret: WAFFLE_STUDIO_SEMINAR_SECRET_KEY
  issuer: Waffle_Studio
  jwtExpiration: 86400